# 録音メモ

話した内容をその場で文字起こしし、Claude API で整形・要約・翻訳できる単一HTMLのWebアプリ。

- 文字起こし: ブラウザ内蔵の音声認識（Web Speech API）— 無料・APIキー不要
- 整形 / 要約 / 翻訳: Claude API（各自のAPIキーを端末内に保存して使用）
- データはブラウザの localStorage にのみ保存され、サーバーには送信されません

iPhone は Safari で開き、「ホーム画面に追加」するとアプリとして使えます。
